version: '3'
services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    restart: always
    container_name: django-app
  
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   container_name: react-app
  #   volumes:
  #     - ./frontend:/app